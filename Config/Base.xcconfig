// Copy Local.xcconfig.example to the ignored Local.xcconfig for private signing values.
// The optional include lets unsigned simulator builds work without developer credentials.

#include? "Local.xcconfig"
