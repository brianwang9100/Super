# macOS
.DS_Store

# Design reference (extracted prototype, not deliverable)
.design-tmp/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
DerivedData/
build/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Generated project (xcodegen output is committed; user-state inside it is not)
# Keep project.pbxproj in git, but exclude per-user state above.

# Per-developer Xcode signing config — see Config/Local.xcconfig.example
Config/Local.xcconfig

# CocoaPods (not used, but defensive)
Pods/

# Carthage (not used, but defensive)
Carthage/Build/

# Editor
.vscode/
.idea/
*.swp
*~

.claude/worktrees/

# Superset workspace setup/teardown config — user-specific, not for the OSS repo
.superset/

# Coverage
*.profraw
*.profdata
coverage/

# Secrets / credentials — defense-in-depth, none of these are tracked today
.env
.env.*
!.env.example
*.p8
*.p12
*.pem
*.key
*.cer
*.crt
*.mobileprovision
*.provisionprofile
.claude/scheduled_tasks.lock
