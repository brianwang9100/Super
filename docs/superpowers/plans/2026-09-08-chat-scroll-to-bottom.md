# Chat scroll-to-bottom implementation plan

**Goal:** Show a centered down-arrow above the composer whenever the transcript is away from its bottom, and let a tap return to the latest content.

**Approach:** Keep geometry observation and explicit scrolling inside `MessageList`, where transcript scrolling already lives. Render a 44-point circular glass `ScrollToBottomButton` as a bottom-centered overlay with an 8-point gap. Observe only whether the remaining scroll distance exceeds a small layout tolerance against the measured transcript viewport. Use `SuperMotion.chrome(hiding:reduceMotion:)` for the same 0.4-second reveal / 0.6-second hide as the chapter controls (0.2 seconds with Reduce Motion). A tap marks the current turn-focus request complete and seeks a bottom target through `ScrollViewReader`; any lazy-layout refinement must be bounded and relinquish ownership on arrival or user interaction. Do not retain a bound `ScrollPosition` bottom edge, which could follow later content growth. Subsequent response growth never scrolls automatically.

**Scope:** Chat package only; no applet or shell API changes. The control inherits transcript visibility and resets with the existing conversation identity. Hidden controls must neither receive taps nor appear in accessibility. Use theme, typography, glass, and haptics conventions already used by `ComposerAccessoryFlank`.

**Risks:** Lazy row measurement must not leave the scroll above the bottom; geometry observation must not create a layout loop; button visibility must respond to streaming growth and viewport changes without changing the reading position. Preserve send/retry/regenerate turn focus and short-response blank space. Verify Reduce Motion and keyboard/composer placement.

## Steps

- [x] Review this plan with an independent review subagent and address actionable findings: use a proxy target seek without a persistent bottom-edge binding. Test growth immediately after the jump and subsequent explicit turn focus.
- [x] Preserve the existing UIKit transcript behavior suite and add four lifecycle tests for completion, later growth, bounded lazy-layout correction, and cancellation. A proposed real-button host test failed because the hosted SwiftUI accessibility tree is empty in the unit-test runner; remove that vacuous harness and verify actual taps through the running app's simulator accessibility tooling.
- [x] Implement the overlay, geometry predicate, and explicit bottom-edge action. Keep animation scoped to button opacity and avoid adding transcript layout space. Isolate visibility observation in the button.
- [x] Add light/dark ChatScreen captures for floating glass contrast over text and centering above the actual composer. Existing captures fit the viewport and cannot show this control. Legacy PNG count: 581 → 583; no retirements, Argos inventory unchanged. Recorded on Xcode 26.4.1 (17E202), iOS 26.4.1 (23E254a), iPhone 17.
- [x] Run the Chat package suite, relevant simulator behavior and visual checks, and lint/diff checks. Review changes with a separate subagent and address findings.
- [ ] Draft PR #342 is created using the repository template. Monitor CI and Codex review every 10 minutes. Mark ready and enable auto-merge only with passing applicable checks and explicit Codex approval of the current revision, then verify merge.

**Validation constraints:** CI currently pins Xcode 26.4.1 and iOS 26.4 build 23E254a on iPhone 17. Use `Scripts/worktree_simulator.py ensure` for this worktree's device. The initially selected local toolchain is Xcode 27.0 (27A5252f); locate the pinned toolchain before recording any baseline.

**Progress:** Full Chat `swift test --parallel`: 1,108 tests passed, including the measured composer-inset regression. SuperBible simulator build passed. Changed Swift files pass SwiftLint. Independent implementation review and the inset correction were approved without actionable findings. Manual QA used 40 varied-height turns: scrolling to response 21 shows the arrow; tapping reaches response 40's end marker and fades it away. A subsequent send stays at the question while the canned response grows; short content hides the arrow. Post-jump ownership release is covered by the request lifecycle tests.

**Final simulator validation:** The full Chat run passed 1,361 tests; the two new snapshots differed by a small glass-edge opacity tail. After disabling animation in those new fixtures and re-recording only their baselines, all 66 tests across the five affected UI suites passed with `TEST SUCCEEDED`. Existing snapshots remain unchanged.

**Existing stress issue:** Entering a roughly 1,900-character composer draft after loading long history can stall native SwiftUI layout. Repeating the identical input with `MessageList.swift` restored to base commit `79b70606` reproduces the same sustained CPU use and native layout stack. This is not introduced by the button; no unrelated composer behavior is changed here.

## PR review follow-up

Codex identified that the existing non-interactive copy confirmation covers the arrow and passes taps through. Place both controls in one bottom-aligned transcript overlay, with the confirmation 8 points above the arrow's permanently reserved 44-point slot. A captured initial attempt showed that keeping the overlays in different parents used different coordinate origins, so both now share the same `MessageList` overlay. Use an opacity-only transition so the pill never slides across the arrow. Include both controls in the new light-theme screen capture and inspect their separation; preserve the dark capture for glass contrast.

Main's completed Argos migration landed during implementation. Merge it, convert the two new fixtures to `verifyVisualSnapshot`, register their names/dimensions in the package inventory, and remove their generated Git PNGs. The final inventory changes from 623 to 625 total captures (Chat 244 to 246). Validate migration/discovery checks, the affected Chat package and simulator suites, and local capture output before pushing and requesting a fresh Codex pass.

**Follow-up validation:** The separate reviewer approved the shared overlay and inspected the combined capture. The final run passed 1,108 Chat macOS tests and 65 simulator tests across the five affected suites (15 screen, 31 transcript capture, 19 scroll behavior/visibility). All 15 ChatScreen and 31 MessageList exported images passed inventory-name, dimension, PNG decoding, and completeness validation. All 23 visual-pipeline tests, discovery, lint, and diff checks passed. PR #342 remains draft pending fresh Codex approval and passing CI for the follow-up revision.

## Lazy-bottom review follow-up

The request currently treats any zero remaining distance as completion, including the lazy stack's provisional estimated bottom; the scroll-visibility callback also cancels on that estimate. Preserve the bounded request through estimated-bottom observations and complete it only against the last rendered turn's bottom geometry. Remove cancellation from the visibility callback. Keep a snapshot of the transcript inputs and viewport associated with the tap so new streaming content, persistence, or resizing cancels the request before it can issue a correction for different content. Retain the four-correction budget and manual/new-turn cancellation.

Add a deterministic regression for provisional zero followed by materialized positive distance, then actual arrival. Test that a changed content snapshot cancels even before actual arrival and cannot restart the request. Run the Chat package and simulator scroll behavior suites, inspect the final capture for unchanged placement, and exercise a long varied-height history in the app where practical. Have an independent reviewer critique the plan and final change before pushing.

**Lazy-bottom validation:** The provisional-bottom regression failed on the prior helper and passes with rendered-target completion. All 1,110 Chat macOS tests and 67 affected simulator tests pass; the two new screen captures are byte-identical to the previously inspected versions, and all 46 screen/transcript exports validate against the inventory. SuperBible simulator build, lint, and diff checks pass. In a synthetic 240-turn varied-height conversation, scrolling back to response 216 and tapping once reaches the visible `LATEST RESPONSE 240 END.` marker; the arrow fades away. The independent implementation reviewer approved the corrected geometry and request lifetime.

## Integration with animated send focus

Main PR #341 replaces the old turn-focus latch with `MessageListFocus`, animated explicit seeks, tagged final geometry, and native scroll-phase handling. Merge that behavior intact, including removal of redundant `scrollTargetLayout`. Keep bottom-jump state in its own non-observable holder. A bottom tap cancels the focus coordinator before beginning its independent immediate jump; a new explicit focus request or manual scrolling cancels the bottom request. Preserve content-snapshot cancellation for bottom jumps without cancelling the new send animation on token growth.

Review this integration plan, resolve the single `MessageList.swift` conflict, and run the exact warnings-as-errors Chat package command plus affected simulator focus/scroll/capture suites. Verify both a history-to-bottom tap and the newly merged send behavior in the running app. Review the merged implementation, push, and require renewed Codex approval and CI for the new head.

**Animated-focus integration validation:** Independent plan and implementation reviews approved the resolved integration. The exact Chat CI command (`swift test --parallel --enable-code-coverage -Xswiftc -warnings-as-errors`, pinned Xcode 26) passed 1,118 tests in 85 suites. All 75 affected simulator tests passed: 29 focus/scroll behavior tests across four suites, 15 ChatScreen captures, and 31 MessageList captures. All 46 exports validate; the two new captures are byte-identical to the previously inspected images. The 23 visual-pipeline tests, capture discovery, SuperBible simulator build, SwiftLint, and diff checks passed. In the merged app, an arrow tap dispatched 133 ms after send settled without a late focus correction; after the prior scroll gesture settled, a subsequent send again positioned its question at the top and kept it there through response completion. A final settled-history arrow tap returned to that latest response. No additional source changes were needed after these checks. Push this merge revision and obtain fresh CI and explicit Codex approval before completing delivery.

## Rebase onto repository snapshot restoration

The user authorized rebasing active work onto main after PR #354 removed Argos and restored repository PNG baselines. Checkpoint the current clean feature revision before rewriting its branch. Rebase onto the latest main, retain the independent bottom and animated-focus behavior, and keep main's snapshot comparison infrastructure and audit tests. Resolve retired Argos documentation against the restored workflow; register and explicitly record only this feature's two intentional light/dark screen baselines. Preserve all 623 main baselines, renderer pins, and the resulting 625-image inventory.

Risks are losing merge-resolution behavior when replaying the feature commits, accidentally restoring Argos infrastructure, and overwriting unrelated baselines. Compare final production sources against the checkpoint, inspect the two recorded images, and verify all other baseline files are unchanged from main. Run the affected Chat package suite with warnings as errors, the focus/scroll simulator suites, and the affected screen/transcript comparisons; run the complete Chat visual comparison to validate its restored 246-image inventory. Obtain independent plan and implementation review. Push with an explicit lease for the previously observed remote head, require fresh Codex approval and applicable CI, and update the existing monitor to use the six remaining protected checks with no Argos dependency.

**Repository snapshot rebase validation:** Rebased onto `ba1161b1` with the original `e1be2111` checkpoint retained. Independent source review confirmed the feature production code is unchanged, with main’s new copy-confirmation sleep seam preserved. The initial comparison and inventory audit failed only for the two missing intentional baselines; explicitly recorded only those two selected tests and inspected both images. Both PNGs are byte-identical to the previously reviewed exports, and all 623 main baseline files remain byte-identical. Default comparison then passed all 246 Chat images across 26 serialized suites and validated the exported bundle. All 1,112 Chat macOS tests, 29 dedicated focus/scroll simulator tests, 27 unchanged visual-pipeline tests, capture discovery, cache-free SwiftLint for nine changed files, and diff checks pass. The same Xcode/iOS/device pins and main’s snapshot infrastructure remain unchanged. The six protected checks are build, lint, gitleaks, ios-test, swift-test, and native-previews; Argos has been removed. Push with the explicit prior-head lease and require fresh current-revision review and CI.

## Preserve in-flight jumps through response growth

Codex found that cancelling on every token or streaming-to-persisted handoff can retire a jump before lazy rows materialize. Tie the pending request to the stable final logical turn and viewport/layout context, rather than the response snapshot. A token update or assistant persistence within that turn must leave the request active until rendered arrival; arrival immediately relinquishes ownership, so subsequent growth remains stationary. Preserve the four-correction budget and cancellation for user scrolling, new focus intent, a different final turn, viewport resize, or explicit thinking/verbosity layout changes. Empty transcripts retain a distinct valid target context.

Add deterministic coverage using actual turn grouping across token and persisted representations: provisional arrival, updated response, rendered correction, rendered arrival, then further growth with no correction. Retain changed-target and manual-navigation cancellation checks. Review this plan and the implementation independently; run Chat's warnings-as-errors suite, affected simulator behavior, and default screen/transcript comparisons without recording. Exercise a live canned response from long history if feasible. Reply to Codex with the concrete fix and evidence, then push and obtain fresh current-head approval and CI.

**Active-stream validation:** The regression failed both token and persistence corrections with the previous full-snapshot identity, then passed with stable turn/layout identity. All 1,113 Chat macOS tests and 76 affected simulator tests pass (30 focus/scroll, 15 screen, 31 transcript comparisons). No PNGs changed. SuperBible simulator build, cache-free changed-file SwiftLint, and diff checks pass. Independent plan and implementation reviews approved the fix. In the real app’s 240-turn QA history, the arrow was tapped from response 237 while a canned response was actively generating; it reached the new question at y=128. Three observations while generation remained active, and another after completion, retained exactly that position. Before/after screenshots confirm the history arrow and active-response destination. The first shorter canned reply completed before the tap and was excluded from this evidence.

## Animate explicit bottom navigation

The user requested animated scrolling when tapping the arrow. Animate the initial bottom seek and any bounded lazy-layout corrections with the existing explicit-focus 0.3-second ease-in-out timing; Reduce Motion keeps positioning immediate. Keep the chapter-matched button fade unchanged. Extend the bottom request to serialize corrections against native scroll phases: intermediate animation frames cannot complete a request, restart its movement, or consume its correction budget. After native motion ends, request a fresh tagged geometry sample before deciding whether the rendered destination has arrived. Reuse the transcript's measurement generation for focus and bottom navigation.

Preserve stable turn/layout identity through streaming and persistence, the four-correction cap, and cancellation on manual scrolling, new focus, or layout/target changes. Animation must relinquish ownership once rendered arrival is confirmed, leaving later tokens stationary. Risks are an immediate geometry correction interrupting the visible animation, stale geometry finishing too early, or a cancelled animation resuming on a late idle callback.

Review this plan independently before implementing. Add deterministic coverage for animation-frame suppression, fresh completion geometry, cancellation, repeated corrections, and immediate Reduce Motion behavior. Run the complete Chat macOS suite, affected simulator behavior and default screen/transcript comparisons, and build the app. Verify visible motion and final arrival in long history, active streaming, interruption, and Reduce Motion using this worktree's simulator. No new or updated PNG baseline is expected because final appearance is unchanged. Obtain independent implementation review, update PR #342, and require new current-head Codex approval and passing CI before resuming the authorized merge steps.

**Animation plan review:** Addressed two edge cases before implementation: retain the minimum accepted geometry generation across both stack and rendered-turn samples, and reconcile seeks that produce no native motion. Each seek carries a movement ID so stale animation completions cannot release newer motion. Transaction completion requests fresh geometry only when native motion has not begun; a late native start restores suppression until idle. The existing native phase is retained so a tap during animated send focus also waits for the real idle event. Add deterministic coverage for these orderings.

**Animated-bottom validation:** All 1,118 Chat macOS tests pass with the exact CI warnings-as-errors command. All 81 affected simulator tests pass (35 focus/scroll, 15 ChatScreen, 31 MessageList comparisons), as do the SuperBible build, changed-file SwiftLint, and diff checks. Five new test methods cover animation-frame suppression, no-motion recovery, stale completion and geometry orderings, delayed native motion, bounded corrections, and cancellation; the previous request behavior failed 23 assertions before implementation. No PNG baseline or capture inventory changed. Independent plan and implementation reviews found no remaining confirmed actionable issues.

Live QA on the pinned worktree simulator recorded distinct intermediate positions during the bottom animation and final arrival with the arrow fading away. A tap during active canned generation reached the new turn at y=127.67 and held exactly that position over three active-stream observations and completion. A manual drag beginning 128 ms after the tap interrupted the animation and remained in history after settling. With the system Reduce Motion switch confirmed on, a history-to-bottom tap positioned the latest turn immediately at y=128; the original off setting was restored. PR #342 must receive renewed current-head Codex approval and CI before delivery.

## Integrate main's navigation history and voice changes

Main advanced to `99d6dac3` and PR #342 now conflicts only in snapshot-count documentation. Merge main while preserving the reviewed animated-bottom sources and all incoming Bible navigation, append-only voice input, test-audit, and workflow updates. Reconcile the combined inventory from actual discovery: main adds two Bible images and this feature adds two Chat images. Update current totals without rewriting historical counts. Verify auto-merged ChatScreen and ChatScreenViewModel retain both voice wiring and the copy-confirmation/scroll feature.

Have the integration plan and resolved diff reviewed independently. Compare this feature's motion files with `113017dd`, validate inventory and baseline identities, run the affected Chat package suite and simulator focus/scroll/screen/transcript comparisons, and build SuperBible. Preserve incoming PNGs without recording. Push the resolved merge only after local validation; renew Codex approval and CI for that revision before resuming delivery.

**Navigation-history integration validation:** Independent plan and implementation reviews approved the merge with `99d6dac3`. Only snapshot-count documentation required manual resolution. The four motion source/test files and two feature PNGs remain byte-identical to `113017dd`; all 636 main PNG files (including non-capture assets) are preserved byte-for-byte. Discovery verifies 586 package captures (Bible 278, Chat 246, Core 20, Todo 42) plus 41 native, totaling 627. Main’s voice subscription, display binding, and recording/send guards coexist with the feature’s copy-confirmation wiring. The final merged revision passes 1,111 Chat macOS tests (main’s test audit changes the total), 81 affected simulator tests, 27 visual-pipeline tests, the SuperBible build, cache-free lint, and diff checks. No baseline recording or motion changes were needed, so the recorded native animation QA remains applicable. Renew current-head CI and Codex approval after pushing this merge.
