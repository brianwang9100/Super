import CoreGraphics

public enum SuperContentLayout {
    /// Includes each surface's horizontal padding so content and floating controls share a column.
    public static let maximumColumnWidth: CGFloat = 760
}
