# Super Chat MVP — Implementation Status

**If you are an AI session or engineer resuming this work: read this file first.** It is the single source of truth for *where we are*. The plan is the source of truth for *what we are doing*.

## Quick links

- **Plan**: `/Users/bwang/.claude/plans/zany-dazzling-willow.md` — the full approved implementation plan (all 13 milestones M0–M12).
- **Project agent rules**: `/Users/bwang/Development/Super/AGENTS.md`. `CLAUDE.md` is a symlink to it.
- **Design reference (canonical visual)**: `/Users/bwang/Development/Super/.design-tmp/chat/project/`
  - `Super.html` — entry point.
  - `src/theme.jsx` — palette tokens (oklch) for Light / Dark / Sepia.
  - `src/chat-view.jsx` — chat screen, composer, header, pills.
  - `src/sidebar.jsx` — drawer.
  - `src/settings.jsx` — settings sheet + panes.
  - `src/message-parts.jsx` — block renderers (text / thinking / tool / code).
  - `src/data.jsx` — seed data (also useful as test fixtures).
  - `chats/chat1.md` — the user/designer chat log that established verbosity semantics, composer shape, etc.
- **Architecture docs** (under `docs/`): `PRODUCT_VISION.md`, `MOBILE_ARCHITECTURE.md`, `Chat/ARCHITECTURE.md`, `CLIENT_SERVER.md`, `SYNC.md`, `AUTH.md`, `CI_PIPELINE.md`, `SECURITY.md`, `OBSERVABILITY.md`, `AI_TOOLS.md`, `DEVELOPMENT_SETUP.md`.

## Current state

- **Active milestone**: M9 — Settings (in_progress, awaiting manual simulator review)
- **Last action**: M10 done — MarkdownUI + Splash now power assistant text, thinking traces, the compaction banner summary, and fenced code blocks. Streaming tail intentionally stays plain `Text` to avoid re-parsing partial markdown on every delta. Code blocks render with the design's dark surface + lang label + copy pill chrome and use Splash for Swift syntax highlighting (other languages degrade to monospaced plain text painted in the code foreground). GFM tables wrap in a horizontal scroll so wide rows don't clip on a 402pt iPhone. 4 new files under `Packages/Chat/Sources/Chat/UI/Markdown/` (MarkdownTheme, SplashHighlighter, CodeBlockView, MarkdownText). 6 new unit tests + 3 new snapshot baselines (`list_markdown_light`, `list_codeblock_light`, `list_table_light`); all 47 pre-existing snapshot baselines re-recorded once due to iOS sim runtime PNG-encoding drift (verified pixel-identical via PIL before re-recording). 241 swift tests / 300 xcodebuild tests across 40 suites pass; `xcodebuild -scheme Super build` succeeds; app installed + launched on iPhone 17 sim (UUID `472D292D-71F0-4D2B-ADFC-C5D5BAF14450`).
- **Last action (prior)**: M7 done + reviewer-pass applied — Chat UI surface landed under `Packages/Chat/Sources/Chat/UI/` with pixel-parity to `.design-tmp/chat/project/src/chat-view.jsx`. Code-reviewer subagent ran post-implementation; all four MUST findings (M-1 Dynamic Type XXL coverage, M-2 fonts not actually scaling, M-3 compaction banner edge cases, M-4 access-level tightening) and most SHOULD findings (S-1 clean snapshot dirs, S-5 cancel UX, S-10 race fix, S-12 sepia hue) applied. 190 tests across 27 suites pass via `swift test --parallel`; 34 snapshot baselines re-recorded on iPhone 17 sim. New types: `SuperTheme` (Light/Dark/Sepia palettes via `OKLCH`→sRGB conversion, accent hue parameterized, injected via `@Environment(\.superTheme)`); leaf views `ChatHeader`, `ChatEmptyStateView` (time-aware greeting via injected `Clock`), `ContextMeter`, `ModelPill`, `VerbosityPill`, `SparkIcon`; composites `ChatComposerFooter`, `ChatComposer` (rounded 26pt capsule, mic↔send/stop trailing button, Enter submits), `MessageListView` (user bubble + assistant text + tool-call card + compaction banner + streaming tail with typing caret + error banner — assistant markdown polish deferred to M10); `ChatScreen` top-level composition; `ChatScreenViewModel` (`@Observable @MainActor final class`) consuming a `ChatSessionDriver` protocol, with `LiveChatSessionDriver` adapting the existing `ChatSession` actor. `App/ContentView.swift` now renders `ChatHostView` which seeds a starter conversation, builds the view model, and applies the theme. Snapshot tests under `Packages/Chat/Tests/ChatTests/UI/Snapshots/` (iOS-gated via `#if canImport(UIKit)`) cover ChatScreen / ChatComposer / MessageListView / ChatHeader / ChatEmptyStateView / ContextMeter — 28 baselines visually verified on iPhone 17 sim. Behavioral tests (`OKLCHTests`, `SuperThemeTests`, `ChatEmptyStateViewGreetingTests`, `ChatScreenViewModelProjectionTests`, `ChatScreenViewModelTests`) cover the pure paths and the view model's state machine. **188 Chat tests across 27 suites pass via `swift test --parallel`** (occasional pre-existing FakeLLMProvider race in single-process mode — see M7 Notes). `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
- **M6 prior summary** (kept for resume context): `TimeNowTool` landed at `Packages/Chat/Sources/Chat/Tools/TimeNowTool.swift` as the only real tool in the MVP. The struct conforms to `ToolExecutor`, exposes the LLM-facing `descriptor` (`time.now`, `query` category, optional `timezone` IANA string parameter), and a `registration(clock:defaultTimeZone:)` factory that returns an enabled `.local` `ToolRegistration` ready to hand to `ToolRegistry`. Clock and default `TimeZone` are injected so tests are deterministic; production uses `SystemClock` + `.autoupdatingCurrent`. Output combines a human-readable `EEEE, MMMM d, yyyy 'at' h:mm:ss a zzz` string with an ISO 8601 timestamp and the resolved zone identifier in one `ToolResult.content`. Invalid IANA names return `isError: true` with a recovery hint instead of throwing, so the LLM can either retry or fall back gracefully. Empty / non-string `timezone` parameters fall back to the default. The Shell composition root (`App/AppBootstrap.swift`) opens `chat.sqlite` under Application Support, builds every repository (`GRDBConversation/Message/ToolCall/CompactionCheckpoint/ModelConfiguration/SettingRepository` + `GRDBToolEnablementStore`), constructs `AppleKeychainClient` + `ToolRegistry` (with `TimeNowTool` registered immediately) + `LLMProviderRegistry` (hydrated from saved `ModelConfigurationRecord`s, selected row registered first so it becomes active per the registry's first-registered-active rule) + `Compactor` + `ChatSessionStore`, and returns an `AppDependencies` value. `App/SuperApp.swift` runs the bootstrap inside a `.task` (state machine `loading | ready | failed`) so any database/Keychain failure surfaces as UI rather than a crashed launch; `App/ContentView.swift` shows the registered tool ids until M7 swaps in the real `ChatScreen`. **162 Chat tests across 22 suites pass** (153 M5-era + 9 new `TimeNowTool` cases). Core stays green (70 tests). `xcodebuild -scheme Super build` for iPhone 17 sim succeeds.
- **Repo root**: `/Users/bwang/Development/Super/`
- **Next concrete sub-step (M8)**: build `SidebarDrawer` per `.design-tmp/chat/project/src/sidebar.jsx`. 300pt left-anchored overlay, "Super" wordmark + version, accent-filled "New Chat" CTA, applet rows (Todo/Recipes/Bible/Finance — visual placeholders), CHATS section listing `ConversationRecord`s sorted by `updatedAt desc` with running-spinner indicator from `ChatSessionStore.runningConversations()`. Active row uses `accentSoft` bg. Floating footer with identity capsule + Settings button (Settings sheet ships in M9). Wire the hamburger button on `ChatHeader` to toggle the drawer (currently a no-op). Snapshot tests for closed / open-empty / open-populated / open-active-highlighted / one-running-chat-spinner × 3 themes.

## Session-resume procedure

1. Read the **Current state** block above.
2. Skim the **Milestone status** table below to see the whole picture.
3. Jump to the in-progress milestone's detail section further down for the latest specific notes (file paths touched, pending sub-steps).
4. Pull the corresponding milestone block out of the plan file for full requirements.
5. Resume.

Do not re-litigate scope. The plan is approved. If something in the plan looks wrong, flag it to the user before changing course — don't silently deviate.

## Milestone status

| # | Title | Status | Updated |
| --- | --- | --- | --- |
| M0 | Project scaffolding | `[x] done` | 2026-04-24 |
| M1 | Core primitives | `[x] done` | 2026-04-24 |
| M2 | Chat persistence | `[x] done` | 2026-04-24 |
| M3 | OpenAI-compatible streaming | `[x] done` | 2026-04-25 |
| M4 | Session orchestration | `[x] done` | 2026-04-25 |
| M5 | Compaction | `[x] done` | 2026-04-25 |
| M6 | Tool system + built-in tool | `[x] done` | 2026-04-25 |
| M7 | Chat UI | `[x] done` | 2026-04-26 |
| M8 | Sidebar drawer | `[x] done` | 2026-04-26 |
| M9 | Settings | `[~] in_progress` | 2026-04-27 |
| M10 | Markdown + code + thinking rendering | `[x] done` | 2026-05-03 |
| M11 | Voice input | `[ ] not_started` | — |
| M12 | End-to-end polish + coverage | `[ ] not_started` | — |

Legend: `[ ]` not started · `[~]` in progress · `[!]` blocked · `[x]` done.

## Update discipline

- **Starting a milestone**: flip its checkbox to `[~]`, set `Status:` to `in_progress`, stamp `Last updated:` to today (absolute date), write a one-line `Notes:` about the first concrete sub-step.
- **Pausing mid-milestone**: update `Notes:` with the latest file touched and the exact next sub-step. Enough detail that a cold reader can resume in under 5 minutes.
- **Finishing a milestone**: flip to `[x]`, set `Status:` to `done`, summarize what landed + where the tests live, update the `Current state` block at the top to point at the next milestone.
- Commit this file in the same PR as the milestone work it describes — never as a standalone "status update" commit.

---

## M0 — Project scaffolding

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-24
- **Notes**:
  - Skipped the plan's SuperBig→Super rename per user direction; repo lives at `/Users/bwang/Development/Super/`.
  - `git init -b main` + `.gitignore` (excludes `.design-tmp/`, `.DS_Store`, SPM build artifacts, Xcode user state).
  - `Packages/Core/` (Swift 6, iOS 18+, no deps) — `Sources/Core/Core.swift` + 1 placeholder test, `AGENTS.md` + `CLAUDE.md` symlink.
  - `Packages/Chat/` (Swift 6, iOS 18+, deps: `GRDB.swift` 7.10.0, `GRDBQuery` 0.11.0, `swift-markdown-ui` 2.4.1, `Splash` 0.16.0; test deps: `swift-snapshot-testing` 1.19.2, `GRDBSnapshotTesting` 0.4.2; local `Core` dep) — `Sources/Chat/Chat.swift` + 2 placeholder tests, `AGENTS.md` + `CLAUDE.md` symlink.
  - `xcodegen` (2.45.4) installed via Homebrew; `project.yml` describes the iOS 18+ `Super` app target linked to both local packages; `xcodegen generate` produces `Super.xcodeproj`.
  - `App/{SuperApp.swift, ContentView.swift, Info.plist, Assets.xcassets/}` placeholders. ContentView prints the wordmark, "Chat MVP scaffolding", and `Core v… · Chat v…`.
  - `.mcp.json` wires `xcodebuildmcp` + `ios-simulator-mcp` (the latter replaces the plan's placeholder "Axiom MCP" — verified npm package). `.claude/settings.json` (project-shared, checked in — note: the plan said `.local.json` but the local-suffixed file is conventionally per-developer and globally git-ignored, so the shared settings live in `settings.json`) allowlists those MCP tools and a small set of safe Bash commands (`swift test`, `xcodebuild`, `xcrun simctl`, `git status/diff/log/add/commit`).
  - `docs/DEVELOPMENT_SETUP.md` §8.5 added to document the MCP tooling story (what each server does, smoke checks, fallback to plain bash).
  - Verifications: `swift test` green in both packages; `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeded; app installed and launched in the iPhone 17 simulator (PID confirmed); screenshot shows the placeholder wordmark.
  - Files added in this milestone (high level): `.gitignore`, `.mcp.json`, `.claude/settings.local.json`, `project.yml`, `Super.xcodeproj/`, `App/{SuperApp.swift, ContentView.swift, Info.plist, Assets.xcassets/*}`, `Packages/Core/{Package.swift, Sources/Core/Core.swift, Tests/CoreTests/CoreTests.swift, AGENTS.md, CLAUDE.md}`, `Packages/Chat/{Package.swift, Sources/Chat/Chat.swift, Tests/ChatTests/ChatTests.swift, AGENTS.md, CLAUDE.md}`, `docs/DEVELOPMENT_SETUP.md` (edited).

## M1 — Core primitives

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-24
- **Notes**:
  - Full Core surface landed under `Packages/Core/Sources/Core/`:
    - `LLM/`: `LLMTypes.swift` (`LLMRole`, `LLMContent`, `LLMMessage`, `LLMModel`, `ModelConfiguration`, `TokenUsage`, `LLMError`), `LLMStreamEvent.swift` (with `.thinkingDelta` — divergence from `MOBILE_ARCHITECTURE.md` §7 to be reconciled in M12), `LLMProvider.swift`, `LLMProviderRegistry.swift` (actor; first-registered-becomes-active).
    - `HTTP/`: `HTTPClient.swift` (protocol + `URLSessionHTTPClient` using `URLSessionDataDelegate` + `HTTPError`), `SSEParser.swift` (buffered, partial-chunk-tolerant; supports both LF and CRLF separators; recognizes `[DONE]`).
    - `Tools/`: `LLMTool.swift` (+ `LLMToolCategory`, `LLMToolParameter`, `ParameterType`), `ToolExecutor.swift` (+ `ToolResult` + `Artifact`), `ToolRegistration.swift` (+ `ToolExecution` enum), `RemoteHTTPToolExecutor.swift` (+ `RemoteToolEndpoint`; scaffolded — registry throws `remoteExecutionNotConfigured` for `.remote` per the plan), `ToolRegistry.swift` (actor; persists via `ToolEnablementStore`).
    - `Ambient/`: `Clock.swift` (`SystemClock` + `FixedClock` using `OSAllocatedUnfairLock`), `IDGenerator.swift` (`UUIDGenerator` + `DeterministicIDGenerator`), `KeychainClient.swift` (`AppleKeychainClient` via Security framework + `InMemoryKeychainClient` shipped in Core for previews/tests), `SuperAppInfo.swift`.
    - `JSON/JSONValue.swift`: Sendable JSON value tree replacing Foundation's non-Sendable `[String: Any]` for tool I/O.
    - `ChatVerbosity` moved to `Packages/Chat/Sources/Chat/ChatVerbosity.swift` — `simple`/`thinking`/`verbose` enum with `atLeast(_:)` ordering, consumed only by Chat's settings pane, system-prompt builder, and view models.
  - Tests landed under `Packages/Core/Tests/CoreTests/` mirroring the source layout: `SSEParserTests` (13 cases — chunk boundaries, CRLF, `[DONE]`, comments, multi-data lines, finish-flush), `URLSessionHTTPClientTests` (5 cases via per-stub-id `URLProtocolStub` so concurrent suites don't trample each other), `LLMProviderRegistryTests` (10 cases — register/unregister/setActive/swap), `ToolRegistryTests` (10 cases — register/enable/disable/lookup/execute, plus enablement-store hydration + persistence), `RemoteHTTPToolExecutorTests` (4), `JSONValueTests` (4), `ClockTests` (4), `IDGeneratorTests` (3), `KeychainClientTests` (5), `SuperAppInfoTests` (2), `LLMToolTests` (4), `ToolRegistrationTests` (3). `ChatVerbosityTests` (4) moved to `Packages/Chat/Tests/ChatTests/`. Total: **73 tests across 13 suites, all green**.
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds — the M0 app target still builds against the expanded Core.
  - Documentation: every public declaration in Core has a `///` doc comment placed directly above it. Multi-parameter or non-obvious functions (`HTTPClient.stream`, `LLMProvider.stream`, `SSEParser.append`/`finish`, `ToolRegistry.execute`/`register`, `RemoteHTTPToolExecutor.execute`) carry `- Parameters:` / `- Returns:` / `- Throws:` markup. Test suites and helpers are documented at the type level. Root `AGENTS.md` gained two new sections: **Source-file documentation** (`///` next to declarations, expand acronyms on first use) and **Swift function declarations** (argument labels, defaults at end, implicit returns, `inout` rules, throws conventions — sourced from `https://docs.swift.org/swift-book/documentation/the-swift-programming-language/functions/`).

## M2 — Chat persistence

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-24
- **Notes**:
  - Source under `Packages/Chat/Sources/Chat/`:
    - `Models/`: `ConversationRecord`, `MessageRecord` (with Chat-owned `MessageRole` enum: `user`/`assistant`/`system`/`tool`, identical case set to Core's `LLMRole` today but a deliberate type boundary so a future provider case or Chat-only row kind becomes an explicit decision in the translation extension rather than silent schema drift), `MessageRole` + `asLLMRole()` / `init(_ llmRole:)` (separate file), `ToolCallRecord` + `ToolCallStatus` enum (`pending`/`executing`/`success`/`failed`/`cancelled`/`awaitingConfirmation`) + `JSONValue` codec helpers (`decodedParameters()`, `decodedResult()`, `static encode(_:)`), `ModelConfigurationRecord` (with `.configuration` projection to Core's `ModelConfiguration`), `ToolEnablementRecord`, `SettingRecord`, `CompactionCheckpointRecord`. All `Codable, FetchableRecord, PersistableRecord, Sendable, Equatable, Identifiable`.
    - `Database/ChatDatabase.swift`: `DatabaseQueue` wrapper with `.open(in:)` (on-disk under `chat.sqlite`) and `.makeInMemory()` (tests). Public `registerChatMigrations(_:)` ships `v1_createTables` covering all 7 tables + indexes (`conversation_on_updatedAt`, `message_on_conversationId_createdAt`, `toolCall_on_{conversationId, messageId, status}`, `compactionCheckpoint_on_conversationId_isLive`, plus the partial unique `modelConfiguration_unique_selected` index that makes the "at most one selected row" invariant a schema law). Cascades: deleting a `conversation` cascades to `message` and `compactionCheckpoint`; deleting a `message` cascades to `toolCall`.
    - `Repositories/`: protocol-typed surface (`ConversationRepository`, `MessageRepository`, `ToolCallRepository`, `ModelConfigurationRepository`, `SettingRepository`, `CompactionCheckpointRepository`) with GRDB conformers, plus `GRDBToolEnablementStore` conforming to Core's `ToolEnablementStore` (so Chat owns persistence while Core stays GRDB-free). `ModelConfigurationRepository` owns the Keychain pairing for API keys via injected `KeychainClient`; `delete(id:)` removes the Keychain entry **first** so a Keychain failure leaves the row in place to retry instead of orphaning a secret no UI handle can reach. `MessageRepository` has no per-message delete by design (documented on the protocol). Live-exclusive invariant on `compactionCheckpoint` enforced inside its write transaction; selected-exclusive invariant on `modelConfiguration` is enforced by the schema-level partial unique index.
    - Column casing unified on `Id` (lowercase d): `modelId` and `toolId` joined the existing `messageId`/`conversationId`/`toolCallId` for in-package consistency.
  - Tests under `Packages/Chat/Tests/ChatTests/`:
    - `Database/ChatDatabaseMigrationTests` (7 cases — table set, index inventory, `message` column shape, FK cascade, idempotency, partial unique index, plus a `GRDBSnapshotTesting` `dumpContent` snapshot of the full v1 schema).
    - `Models/ToolCallRecordTests` (4 — encode/decode round-trip, nil-result for pending row, invalid-JSON throw).
    - `Models/MessageRoleTests` (5 — raw values, `asLLMRole()`, `init(_ llmRole:)`, round-trip totality, allCases exhaustive).
    - `Repositories/ConversationRepositoryTests` (5), `MessageRepositoryTests` (5), `ToolCallRepositoryTests` (6 — incl. cascade-from-message), `ModelConfigurationRepositoryTests` (9 — selected-exclusive via partial unique index, Keychain pairing, URL round-trip, projection), `GRDBToolEnablementStoreTests` (3), `SettingRepositoryTests` (5), `CompactionCheckpointRepositoryTests` (5 — single-live invariant, conversation scoping).
    - **Total: 60 Chat tests across 11 suites, all green.** Core stays green (70 tests).
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
  - Documentation: `///` doc comments on every public declaration; non-obvious functions carry parameter docs; record-level docs explain `apiKeyRef` is a Keychain ref (never the key itself), `parameters`/`result` on `ToolCallRecord` are JSON strings (caller serializes via `ToolCallRecord.encode(_:)`), the live-checkpoint semantics on `CompactionCheckpointRecord`, and that `MessageRole` is Chat-owned with an `asLLMRole()` translation. `docs/Chat/ARCHITECTURE.md` §3 documents the `MessageRole` enum + translation extension and explains the schema-independence rationale; §5 orchestrator samples use `.toolResult` to match.
  - Acronyms expanded on first use per project doc rules: SSE (Server-Sent Events), LLM (Large Language Model), JSON (JavaScript Object Notation), UI (User Interface), API (Application Programming Interface), FK (foreign key), SQL (Structured Query Language).

## M3 — OpenAI-compatible streaming

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-25
- **Notes**:
  - Source under `Packages/Chat/Sources/Chat/LLM/`:
    - `OpenAIWireTypes.swift` — internal Codable shapes for the OpenAI Chat Completions API (Application Programming Interface). Snake_case JSON ↔ camelCase Swift via `JSONEncoder/Decoder` key strategy. Request side: `OpenAIChatRequest` (with `streamOptions.includeUsage = true` so we get real token counts), `OpenAIRequestMessage`, `OutgoingToolCall`, `OpenAITool`, `OpenAIFunctionDefinition`. Response side: `OpenAIStreamChunk`, `OpenAIStreamChoice`, `OpenAIDelta` (carries `content`, `reasoningContent`, `reasoning`, `toolCalls`), `OpenAIToolCallDelta`, `OpenAIFunctionDelta`, `OpenAIUsage`.
    - `OpenAIStreamReducer.swift` — stateful struct turning chunks into `LLMStreamEvent`s. Owns: `emittedMessageStart` (single-fire across chunks), `nextBlockIndex` (our own monotonic block-index counter, distinct from OpenAI's choice index and per-call tool index), `openTextBlock` / `openThinkingBlock` (the currently open block of each kind, if any), `toolCallBuilders` (per-tool-index argument-fragment accumulators), `capturedUsage` (terminal token counts), `emittedComplete` (idempotency guard for `finish()`). Transitions: thinking arrives → close any open text block; text arrives → close any open thinking block; finishReason → close all open content blocks then flush tool calls in OpenAI-index order. Each tool call gets its own contentBlockStart/toolUse/contentBlockStop triplet. Malformed tool-call argument JSON throws `LLMError.decodingFailed` (a broken tool call breaks the turn semantically — no point in papering over it).
    - `OpenAICompatibleLLMProvider.swift` — public `LLMProvider` conformer. `init(id:displayName:model:baseURL:apiKey:http:)` (designated) plus `init(configuration:apiKey:http:)` (projects from `ModelConfiguration`). `apiKey` is optional so unauthenticated local servers (Ollama/LM Studio/MLX) just work — the `Authorization` header is omitted when nil/empty. Builds the URLRequest (`POST {baseURL}/chat/completions`, `Content-Type: application/json`, `Accept: text/event-stream`), drives `HTTPClient.stream` → `SSEParser` → JSON decode → reducer → yields `LLMStreamEvent`s. HTTP error mapping: 401/403 → `.unauthorized`, 429 → `.rateLimited`, other 4xx/5xx → `.providerError(code:message:)`. Tool parameters project to JSON Schema with `bool` → `boolean` translation at the boundary (Swift-friendly enum case names diverge from JSON Schema vocabulary). Tool-result `LLMMessage`s become per-result OpenAI `role: "tool"` rows with `tool_call_id`. Stream cancellation via `continuation.onTermination = { task.cancel() }`.
  - Tests under `Packages/Chat/Tests/ChatTests/LLM/`:
    - `OpenAIStreamReducerTests` (12 cases — messageStart-once, text deltas open & stream a block, reasoning opens a thinking block before text, text after thinking closes thinking and opens text, recognizes both `reasoning_content` and `reasoning` field names, tool-call argument fragments accumulate and decode to `JSONValue`, multiple tool calls flush in index order, malformed tool args throw `.decodingFailed`, `finish()` emits `messageComplete` with captured usage, `finish()` is idempotent, missing usage emits zero counts).
    - `OpenAICompatibleLLMProviderTests` (14 cases — fixture replay for plain/reasoning/toolcall/reasoning+tools, partial-chunk delivery via 17-slice subdivision (exercises the SSE parser), unsupported model throws before any HTTP request, 401→unauthorized / 429→rateLimited / 503→providerError mapping, malformed SSE data throws `.decodingFailed`, request URL and method, Authorization header presence/absence, body shape (model, messages, stream, temperature, stream_options.include_usage, tools), assistant-with-tool-uses + tool-result message round-trips encode correctly).
    - Helpers in `LLM/Helpers/`: `FakeHTTPClient` (records the issued `URLRequest`, replays configured byte chunks; `fromFixture(_:chunkCount:)` convenience splits a fixture into N slices to test partial-frame handling) and `FixtureLoader` (loads from `Bundle.module`'s `Fixtures/` subdirectory).
    - Fixtures (checked into `Packages/Chat/Tests/ChatTests/Fixtures/`, declared as `.copy("Fixtures")` resources on the test target): `openai-plain.txt`, `openai-reasoning.txt`, `openai-toolcall.txt`, `openai-reasoning-and-tools.txt`. Each follows the real OpenAI SSE wire format including `data: {...}\n\n` framing, the role-only first chunk, the trailing `data: [DONE]` sentinel, and a separate usage-only chunk.
    - Package.swift now declares `exclude: ["Database/__Snapshots__"]` to clear the M2-era snapshot resource warning.
    - **Total: 86 Chat tests across 13 suites, all green.** Core stays green (70 tests).
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
  - Documentation: `///` doc comments on every public declaration and on the test suites; key acronyms expanded on first use per project doc rules: SSE (Server-Sent Events), JSON (JavaScript Object Notation), API (Application Programming Interface), HTTP (HyperText Transfer Protocol), I/O (Input/Output), BYOK (Bring Your Own Key).
  - Divergence from the plan path (`Packages/Chat/Tests/Fixtures/`): fixtures live under `Packages/Chat/Tests/ChatTests/Fixtures/` so they ride with the test target as a resource bundle (`Bundle.module`). The plan's path was a sketch; the SPM-conformant path is what actually ships.

## M4 — Session orchestration

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-25
- **Notes**:
  - Source under `Packages/Chat/Sources/Chat/Orchestration/`:
    - `ChatEvent.swift` — view-facing stream surface (`userMessageSaved`, `textDelta`, `thinkingDelta`, `toolCallStarted/Completed/Failed`, `assistantMessageSaved`, `error`). Always-finishes contract: failures arrive as `.error(LLMError)` immediately before stream termination, never as a thrown exception.
    - `ChatSession.swift` — actor; one per conversation. Public surface: `init(conversationId:messageRepository:toolCallRepository:llmProviderRegistry:toolRegistry:clock:idGenerator:)`, `send(text:model:temperature:) -> AsyncStream<ChatEvent>`, `cancel()`, `waitUntilFinished()`, `isStreaming`. Turn loop: write the user `MessageRecord` → loop { fetch all `MessageRecord` + `ToolCallRecord` rows for the conversation → assemble `[LLMMessage]` (assistant rows fold their tool-call rows in as `.toolUse` blocks; tool-result rows look up their originating call's `.failed` status to set `isError`) → drive `provider.stream(...)` with the currently-enabled tools (`toolRegistry.enabledTools(for:provider)` so disabled tools never reach the LLM) → buffer text in memory and persist the assistant `MessageRecord` only on `.messageComplete` (per ADR-BB-003) → save one `ToolCallRecord(.pending)` per requested call → execute each via `ToolRegistry` (status transitions `.executing` → `.success`/`.failed`) and write a tool-result `MessageRecord` (role `.tool`, `toolCallId` linkage) → loop }. The second `send(...)` while a turn is in flight cancels the prior task before starting a new one. `CancellationError` from inside tool execution is re-thrown (not caught as a tool failure) so the session terminates with `.error(.cancelled)` rather than marking the call `.failed`. Provider's `LLMError`s are forwarded as `.error` events. Missing active provider surfaces as `.error(.requestFailed(...))`.
    - `ChatSessionStore.swift` — actor; the app-level singleton over `[ConversationID: ChatSession]`. `session(for:)` is get-or-create (same instance on repeat calls so a streaming turn re-attaches when the view re-mounts). `cancel(for:)`, `shutdown()` (cancels every session and drops them), `runningConversations()` (sorted ids of sessions whose `isStreaming` is true; sidebar reads this for the per-row spinner).
    - Both actors take injected `Clock` and `IDGenerator` (Core's `Ambient/`) so tests can substitute deterministic doubles instead of calling `Date()` / `UUID()`.
  - Tests under `Packages/Chat/Tests/ChatTests/Orchestration/`:
    - Helpers (`Orchestration/Helpers/`): `FakeLLMProvider` (final class wrapping a private actor `FakeLLMProviderState`; `enqueue(_:)` queues per-turn `[LLMStreamEvent]` scripts; `capturedRequests()` records every issued request including model id, messages, tools, temperature; missing-script fallback returns a benign two-event sequence so forgotten enqueues don't hang). `FakeToolExecutor` (configurable `ToolResult` or `FakeToolError`; records every input). `OrchestrationFixtures` (in-memory `ChatDatabase`, default `LLMModel`, `seedConversation(...)`, plus `MonotonicClock` — auto-advances by 1 ms per `now()` call so back-to-back GRDB rows have strictly-increasing `createdAt` for the order-by-`createdAt` fetch).
    - `ChatSessionTests` (9 cases — user message persisted before any assistant write, text deltas accumulate and assistant saves once on `.messageComplete`, intermediate text deltas never write to DB (ADR-BB-003 regression test), provider `.error` event ends the turn with `.error` and no assistant row, missing active provider emits `.requestFailed`, prior messages pass to provider in chronological order, temperature forwards to provider, thinking deltas surface as `.thinkingDelta` events, `assistantMessageSaved` event carries the persisted row).
    - `ChatSessionStoreTests` (5 cases — same instance on repeat `session(for:)`, distinct instances per conversation, two sessions run in parallel to completion, cancelling one session does not affect siblings (synchronizes with a `SleepingToolExecutor`'s `awaitFirstCall()` so the cancellation lands inside `Task.sleep` deterministically), `shutdown()` cancels all and drops sessions).
    - `ChatSessionToolLoopTests` (4 cases — happy-path two-turn loop with tool execution, second-turn history correctly carries `.toolUse` and `.toolResult` blocks back to the LLM, tool execution failure marks `ToolCallRecord.failed` and feeds `isError: true` back to the LLM in the next turn, disabled tools are filtered before reaching the provider via `ToolRegistry.enabledTools(for:)`).
    - **Total: 115 Chat tests across 16 suites, all green.** Core stays green (70 tests).
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
  - Documentation: `///` doc comments on every public declaration; turn-loop semantics, ADR-BB-003 rationale, cancellation behavior, and `MonotonicClock` justification all explained inline. Acronym expansions on first use per project doc rules: ID (Identifier), LLM (Large Language Model).
  - Divergence from the plan path (`Packages/Chat/Sources/Chat/Sessions/`): folder is `Orchestration/` to match the Chat module's `CLAUDE.md` directory convention.

## M5 — Compaction

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-25
- **Notes**:
  - Source under `Packages/Chat/Sources/Chat/Orchestration/`:
    - `TokenEstimator.swift` — `TokenEstimator` protocol + `HeuristicTokenEstimator` (chars/4, rounds up so 1–4 chars → 1 token). Default extension sums per-block cost across `[LLMMessage]`; tool-use payloads stringify their `JSONValue` input (sorted keys for stability) so verbose argument lists show up in the budget.
    - `ContextAssembler.swift` — `ContextAssembler` struct + `ContextAssembly` value (`messages`, `totalTokens`, `maxTokens`, `ratio`, `isOverThreshold(_:)`). Walks newest-first to find the cutoff implied by `checkpoint.uptoMessageId` (inclusive); messages at or before are dropped and the live checkpoint's `summary` is folded in as a synthetic system message ("Summary of earlier conversation (compacted):\n\n…") prepended to the kept tail. Any leading `.system` rows that the checkpoint window covered are re-emitted in front of the synthetic summary so the user's system prompt isn't summarized away. Unknown `uptoMessageId` falls back to the full history (lossy ignore beats silent truncation). Misconfigured `maxTokens <= 0` returns `ratio = 0` and never flags over-threshold.
    - `Compactor.swift` — `Compactor` actor + `CompactorError` (`emptySummary`, `llmError`). `compact(conversationId:messages:toolCalls:priorCheckpoint:model:keepMostRecent:)` returns the persisted new checkpoint (or `nil` when the post-checkpoint history has ≤ `keepMostRecent` messages — default 4). Stale `priorCheckpoint` (an `uptoMessageId` not in `messages`) falls back to the full message list. `wouldCompact(messages:priorCheckpoint:keepMostRecent:)` is a `nonisolated` pure predicate that mirrors the same `messagesToSummarize` slicing — used by `ChatSession`'s pre-flight so the two paths cannot diverge. Builds a summarization prompt: 3–8-sentence-summary system instruction, optional prior summary as a second system anchor, the projected `[LLMMessage]` slice to summarize, then a final user "summarize per the system instructions" trigger. Drives `provider.stream(...)` at temperature 0.2 (no tools), accumulates `.textDelta` into `summary`, throws on `.error` or empty result. Persisted checkpoint records `tokensBefore` (estimator over the full prompt sent to the LLM) and `tokensAfter` (estimator over the trimmed summary). `CompactionCheckpointRepository.save(_:)` demotes any prior live row in the same write transaction.
    - `SlashCommand.swift` — `SlashCommand` enum (currently just `.compact`); `init?(rawText:)` is whitespace-tolerant.
    - `ChatEvent.swift` — adds `.compactionStarted` and `.compactionCompleted(CompactionCheckpointRecord)` cases.
    - `ChatSession.swift` — new injected dependencies: `checkpointRepository`, `contextAssembler`, `compactor`, plus runtime-mutable `autoCompactEnabled` (default true) and `autoCompactThreshold` (default 0.75). New public `compact(model:)` returns its own `AsyncStream<ChatEvent>`. `send(text:model:)` first checks `SlashCommand(rawText:)` and routes to `compact(model:)` on `/compact` (no user `MessageRecord` is written). The turn loop calls `maybeAutoCompact(...)` before each turn — pre-flights via `ContextAssembler.isOverThreshold(autoCompactThreshold)` then runs `runCompactionPass(...)` which gates `.compactionStarted` on actually having something to summarize (prevents empty-banner flashes). `setAutoCompactPolicy(enabled:threshold:)` lets M9 settings push runtime updates into a long-running session. `assembleHistory(model:)` now delegates to `ContextAssembler` and runs the three repository fetches concurrently via `async let`.
    - `ChatSessionStore.swift` — threads `checkpointRepository`, `contextAssembler`, `compactor`, `autoCompactEnabled`, `autoCompactThreshold` into every `ChatSession` it creates.
  - Tests under `Packages/Chat/Tests/ChatTests/Orchestration/`:
    - `TokenEstimatorTests` (6 cases — empty → 0, short rounds up, English ratio, code overshoots within 4:1 bound, message rollup across all block kinds, tool-use input contributes to budget).
    - `ContextAssemblerTests` (8 cases — no checkpoint, with checkpoint drops covered messages and prepends summary, unknown checkpoint id falls back to full history, threshold classification at multiple ratios, ratio = 0 on invalid maxTokens, empty messages corner, **leading `.system` rows preserved across compaction**).
    - `CompactorTests` (8 cases — checkpoint persisted with correct uptoMessageId/summary/before-after token counts, prior live demoted in same transaction, returns nil when too few messages, empty provider summary throws `.emptySummary`, provider `.error` event bubbles as `.llmError`, summarization prompt carries prior summary verbatim, **stale prior checkpoint falls back to full history (predicate agrees)**, **`keepMostRecent` boundary: exactly 3 messages with `keepMostRecent = 2` summarizes one row**).
    - `ChatSessionCompactionTests` (5 cases — auto fires when over threshold and emits compactionStarted before completed before assistantSaved, disabling suppresses it even with same history, manual `/compact` works at any usage level and writes no user row, manual `/compact` with empty history is silent no-op, two concurrent sessions compact different conversations without racing the checkpoint table; **plus a negative regression: small history + small tool result → no spurious compaction event mid-tool-loop**).
    - `SlashCommandTests` (4 cases — exact match, whitespace tolerance, plain text returns nil, unknown commands return nil).
    - `Helpers/OrchestrationFixtures` gained `makeCompactor(database:llmRegistry:clock:idGenerator:)` so the existing test setups don't have to repeat the wiring.
  - Existing `ChatSession*` test setups updated to pass the new dependencies (and `autoCompactEnabled: false` so they keep their original turn-loop semantics — none of them were designed around compaction).
  - **Total: 153 Chat tests across 21 suites, all green.** Core stays green (70 tests).
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
  - `Scripts/ChatLiveLLM/Sources/ChatLiveLLM/ChatLiveLLM.swift` updated: wires `GRDBCompactionCheckpointRepository` + `Compactor` into the in-memory store and renders the new `.compactionStarted` / `.compactionCompleted(...)` events to stdout.
  - Documentation: `///` doc comments on every public declaration (`TokenEstimator`, `HeuristicTokenEstimator`, `ContextAssembly`, `ContextAssembler`, `Compactor`, `CompactorError`, `SlashCommand`, the new `ChatEvent` cases, the new `ChatSession`/`ChatSessionStore` parameters). Acronyms expanded on first use per project doc rules: BPE (Byte-Pair Encoding), JSON (JavaScript Object Notation).
  - Settings keys (`autoCompactEnabled`, `autoCompactThreshold`) are wired through the constructor today; the M9 settings pane will read them from `SettingRecord` and call `ChatSession.setAutoCompactPolicy(enabled:threshold:)` to propagate runtime changes.

## M6 — Tool system + built-in tool

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-25
- **Notes**:
  - Source: `Packages/Chat/Sources/Chat/Tools/TimeNowTool.swift` — `TimeNowTool` struct conforming to `Core.ToolExecutor`, exposing:
    - `static let toolID = "time.now"`, `static let appletID = "chat"`.
    - `static var descriptor: LLMTool` — `query` category, optional `timezone` `string` parameter (IANA identifier; defaults to user's current zone when omitted). Description tells the LLM both what to ask the tool and how to phrase the `timezone` argument.
    - `static func registration(clock:defaultTimeZone:) -> ToolRegistration` — composition-root convenience that wraps the tool in an enabled `.local` `ToolRegistration`.
    - `init(clock:defaultTimeZone:)` — `Clock` and default `TimeZone` are injected so tests are deterministic; production uses `SystemClock` + `.autoupdatingCurrent` so the zone follows Settings without a relaunch.
    - `execute(input:)` — resolves the requested zone, formats the current `Date` as both human-readable (`EEEE, MMMM d, yyyy 'at' h:mm:ss a zzz`, `en_US_POSIX` for stability) and ISO 8601, returns one combined `ToolResult.content`. Invalid IANA identifiers return `isError: true` with a recovery hint instead of throwing — the LLM can fall back to UTC or re-ask the user. Empty / non-string `timezone` parameters fall back to the default zone.
  - Tests: `Packages/Chat/Tests/ChatTests/Tools/TimeNowToolTests.swift` (9 cases — descriptor shape, registration shape, default-zone fallback when parameter missing, IANA override, UTC shortcut (with note that Foundation canonicalizes "UTC" → "GMT" on the resolved zone's identifier), invalid identifier returns `isError` without throwing, non-string parameter falls back to default, whitespace/empty string falls back to default, clock advance changes reported time).
  - Composition root: new `App/AppBootstrap.swift` (`enum AppBootstrap` + `struct AppDependencies`) opens `chat.sqlite` under Application Support, builds `AppleKeychainClient` + every repository (`GRDBConversation/Message/ToolCall/CompactionCheckpoint/ModelConfiguration/SettingRepository` + `GRDBToolEnablementStore`), constructs `ToolRegistry` (with `TimeNowTool.registration()` registered immediately), `LLMProviderRegistry` (hydrated from saved `ModelConfigurationRecord`s — selected row registered first so it becomes active per the registry's first-registered-active rule; missing Keychain entries fall through with `apiKey: nil` so local servers still register), `Compactor`, and `ChatSessionStore`.
  - Shell: `App/SuperApp.swift` now drives a 3-state machine (`loading | ready(AppDependencies) | failed(String)`); the bootstrap runs in a `.task` so a database/Keychain failure surfaces as UI instead of a launch crash. `App/ContentView.swift` is still a placeholder (M7 swaps in the real `ChatScreen`) but now renders a `ProgressView` during `loading`, an inline error pane on `failed`, and the registered tool ids on `ready` — so we can confirm by inspection that `time.now` got registered when the app boots in the simulator.
  - Notes from M1/M2 still hold: `RemoteHTTPToolExecutor` is implemented + unit-tested in Core but no `ToolRegistration` shipping uses `.remote` in v1; `GRDBToolEnablementStore` already conforms to Core's `ToolEnablementStore`.
  - **Total: 162 Chat tests across 22 suites, all green.** Core stays green (70 tests).
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds (regenerated `Super.xcodeproj` via `xcodegen generate` after adding `App/AppBootstrap.swift`).
  - Documentation: `///` doc comments on every public/internal declaration. Acronym expansions on first use per project doc rules: IANA (Internet Assigned Numbers Authority).
  - Reviewer fixes applied: (1) `AppBootstrap.hydrateProviders` now registers in stable `createdAt` order then calls `LLMProviderRegistry.setActive(id:)` explicitly instead of relying on the registry's first-registered-active rule; (2) `AppBootstrap.bootstrap(directory:keychain:)` now takes an injectable keychain so a future bootstrap test can swap in `InMemoryKeychainClient` without touching the user's real Keychain; (3) `TimeNowTool.descriptor`'s `timezone` parameter description now explicitly tells the LLM "Omit this parameter unless the user explicitly named a specific city, region, or timezone" to discourage small frontier models from over-eagerly defaulting to UTC; (4) `descriptor` is now a stored `static let` (matches its constant-value doc claim).
  - **Known gap (deferred)**: no `AppBootstrap` integration test exists — the App target lacks a test target in `project.yml`, and adding one is M7-adjacent work. The `keychain` injection seam landed in this milestone so the test is cheap to write later. M7 should add an `App/Tests/` target (or move `AppBootstrap` + `AppDependencies` into the Chat package and test from `ChatTests`) and cover at minimum: fresh DB → `registeredToolIDs == ["time.now"]`, `enabledTools().map(\.id) == ["time.now"]`, `activeID() == nil`. Until then the only thing exercising the bootstrap is `xcodebuild` + manual simulator launch.

## M7 — Chat UI

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-26
- **Notes**:
  - Source under `Packages/Chat/Sources/Chat/UI/`:
    - `Theme/OKLCH.swift` — OKLCH (Oklab Lightness-Chroma-Hue) → sRGB Color converter following the published Oklab transform (Björn Ottosson 2020). Channel clamping to `[0, 1]` keeps out-of-gamut accent hues from producing negative components SwiftUI rejects.
    - `Theme/SuperTheme.swift` — palette struct (~25 named tokens mirroring `.design-tmp/chat/project/src/theme.jsx`). `SuperTheme.make(.light/.dark/.sepia, accentHue:)` builds the three theme variants once at construction time (no per-frame conversion). `EnvironmentKey + var superTheme: SuperTheme` exposes it; `View.superTheme(_:)` injects it AND pins `preferredColorScheme` so iOS chrome (status bar, native pickers, blur) adopts the matching variant. Accent hue is parameterized — M9 settings will rebuild the active theme on slider drag.
    - `Icons/SparkIcon.swift` — 12-spoke radial via `Canvas`, alternating short/long, scales with the size parameter. Mirrors `IconSpark` in `icons.jsx`.
    - `ChatHeader.swift` — sticky 60pt-tall header. Hamburger button (40×40 raised circle) on the leading edge, centered title (15.5pt medium, max 240pt with tail truncation), trailing 40×40 spacer for visual centering without measured layout. Background uses `theme.background.opacity(0.85) + .ultraThinMaterial` per the design's `backdrop-filter: blur(16px) saturate(1.4)`.
    - `ChatEmptyStateView.swift` — centered 36pt accent spark + 26pt Instrument Serif greeting (max 300pt width). Greeting derived from injected `Clock` via `static greeting(for:calendar:)` — pure function with four hour-of-day branches (`<5 → tonight`, `<12 → morning`, `<17 → afternoon`, `<21 → evening`, else `tonight`). Tests cover every branch.
    - `ContextMeter.swift` — 26×3pt thin bar (border background, accent fill at `min(1.0, used/max)`) + JetBrains Mono `%.1fK / %.0fK` label. Accessibility-collapsed to a single "Context: X of Y tokens" element so VoiceOver doesn't read the bar separately.
    - `ModelPill.swift` — `Menu`-backed dropdown over `[ModelPill.Option]`. Shared `FooterPillLabel` (capsule with faint border, soft ink, trailing chevron) reused by `VerbosityPill` so both pills look identical. Native `Menu` replaces the React popover for free haptic + a11y.
    - `VerbosityPill.swift` — `Menu` over `ChatVerbosity.allCases`. Each row carries a label and a subtitle (`Just the answer` / `Show thinking` / `Show everything`) sourced verbatim from the design.
    - `ChatComposerFooter.swift` — pills + flexible spacer + meter row, no logic of its own beyond layout. Composer hosts the trailing button so its enabled/disabled state can change without re-laying out the row.
    - `ChatComposer.swift` — rounded 26pt capsule with a `TextField(axis: .vertical, lineLimit: 1...6)` + the `ChatComposerFooter` + a single trailing 34pt circle button that flips between **mic** (empty composer, returns soft-bg circle), **send** (non-empty, accent-filled with up-arrow icon), or **stop** (during streaming, accent-filled with stop icon). Enter submits via `submitLabel(.send) + onSubmit`; Shift-Enter inserts a newline natively (`axis: .vertical`). Slash command parsing happens upstream — composer just forwards trimmed text. Focus ring uses `accent @ 12% opacity` glow (matches the design's `box-shadow: 0 0 0 4px color-mix(...)`). The composer container is wrapped in a vertical gradient (`background → background @ 0%`) for the soft fade above it.
    - `MessageListView.swift` — projects `MessageListView.Item` cases (`userBubble`, `assistantText(text, toolCalls)`, `compactionBanner`) into a `ScrollView + LazyVStack`, plus an optional `StreamingTail` (live thinking + text + isCompacting flag) appended after persisted rows, plus an optional `ErrorBanner`. `UserBubbleView` uses an `UnevenRoundedRectangle` (18pt corners, 6pt bottom-trailing) to match the design's chat bubble shape. `AssistantMessageView` lays out tool-call cards above the text and Copy/Regenerate icon buttons below. `ToolCallBlockView` defaults expanded only in `.verbose` verbosity (matches the design); collapsed shows the tool name in monospace + a "running"/"done"/"failed" status badge; expanded shows INPUT/RESULT JSON in monospace blocks. `CompactionBannerView` is the M7 minimal banner (M10 polishes the tap-to-expand summary). `ThinkingBlockView` is the M7 minimal card; M10 swaps in MarkdownUI for the body. `TypingCaret` is a 7×14pt accent capsule that blinks via `withAnimation(.linear.repeatForever)` and respects Reduce Motion (solid, no blink, when the accessibility setting is on). `ErrorBannerView` matches the warm-red banner from the design with a "Retry" pill button. `ScrollViewReader` watches `items.count` and `streamingTail` and auto-scrolls to bottom on change.
    - `ChatScreen.swift` — top-level `VStack` of header / content (empty state when no messages and no streaming, list otherwise) / composer. Drives `viewModel.load()` from `.task`. Hamburger `onMenuTap` is a parameter the host wires to a no-op until M8.
  - View model: `Packages/Chat/Sources/Chat/ViewModels/ChatScreenViewModel.swift` (`@Observable @MainActor final class`) exposes `items`, `streamingTail`, `error`, `composerText`, `selectedModelId`, `verbosity`, `usedTokens`, `isStreaming`, `headerTitle`. `send(_:)` no-ops on empty / streaming / no model; otherwise spawns a streaming task that consumes the `ChatSessionDriver.send(...)` AsyncStream and folds each event into observable state. `userMessageSaved` / `assistantMessageSaved` / tool events trigger a transcript refresh from the message + tool-call + checkpoint repos; `textDelta` / `thinkingDelta` accumulate into the streaming tail; `compactionStarted` / `compactionCompleted` flip the tail's `isCompacting` flag; `error(LLMError)` surfaces a human-readable banner. Pure helpers (`project(messages:toolCalls:checkpoint:)`, `mapStatus`, `describe`) are `nonisolated static` so callers can run them without an actor hop. `_setSnapshotState(...)` is a documented test seam for snapshot tests + previews.
  - Driver indirection: `ViewModels/ChatSessionDriver+Adapter.swift` defines `LiveChatSessionDriver` wrapping the existing `ChatSession` actor. The protocol indirection keeps the view model free of provider-specific knobs (e.g. `temperature`) and lets snapshot tests substitute a `NoopDriver` without subclassing an actor.
  - Composition root: `App/ContentView.swift` rewritten. `ChatHostView` builds the view model from `AppDependencies`, seeds a starter `ConversationRecord` if none exists (M8 sidebar will replace this with explicit "New chat" + selection), wires the live `ChatSessionDriver`, applies the theme via `superTheme(_:)`. `LoadingPane` and `FailurePane` are extracted helpers. `BootstrapState.failed` still renders cleanly without a theme.
  - **Reviewer fixes applied (2026-04-26)**: code-reviewer subagent ran on M7 and returned MUST + SHOULD findings. Applied:
    - **M-2 fonts → Dynamic Type**: every `.font(.system(size: N))` callsite in `MessageListView`, `ChatComposer`, `ChatHeader`, `ModelPill`, `VerbosityPill` migrated to `.font(.system(.subheadline / .body / .footnote / .caption / .caption2))` so iOS scales Chat text with Dynamic Type. The chevron in `FooterPillLabel` is the one remaining fixed-size point because it's decorative and would look broken at XXL.
    - **M-3 compaction banner edge cases**: `ChatScreenViewModel.project(...)` rewritten to use an "armed" flag walked alongside `messages` instead of comparing `cutoffId` to `items.last.id`. Now correctly emits the banner when (a) the cutoff lands on a `.system` or `.tool` row that the projection drops, and (b) the cutoff is the most-recent persisted message (banner now lands at the tail). Two new projection tests cover both.
    - **M-4 access tightened**: `_setSnapshotState(...)` is now `internal` (test target uses `@testable import Chat`); the SDK surface no longer exposes the snapshot test seam.
    - **M-1 + S-1 + S-2 snapshot matrix**: every snapshot suite gained at least one `dynamicTypeSize(.xxLarge)` variant per AGENTS.md §Testing.2; all helpers thread `function: String = #function` → `verifySnapshot(testName:)` so on-disk paths are now `__Snapshots__/<Suite>/<testFunc>.<name>.png` instead of mangled helper signatures. Reduce Motion: `\.accessibilityReduceMotion` is read-only in SwiftUI's environment so a snapshot variant isn't possible; the steady-state first frame is identical to the animated one anyway (caret starts visible). The conditional in `TypingCaret` covers the runtime branch. Documented in `MessageListViewSnapshotTests.swift`.
    - **S-5 cancel UX**: `ChatScreenViewModel.handle(.error(.cancelled))` now clears the banner instead of surfacing "Stopped." — that path only fires when the user taps the stop button, which isn't a failure.
    - **S-10 ensureViewModel race-proofing**: `ChatHostView` now uses `@State private var bootstrapStarted = false` set on entry to the critical section so two `.task` firings (scene refresh, identity change) can't both pass the nil-check before either assigns `viewModel`.
    - **S-12 sepia accent hue**: `SuperTheme.make(.sepia)` defaults `accentHue` to 80° (matches `--accent` in `theme.jsx`), not 45° as previously shipped. Sepia baselines re-recorded.
    - All 34 snapshot baselines re-recorded on iPhone 17 (iOS 26.3) sim with the new fonts + sepia hue + clean directory names. **190 Chat tests across 27 suites pass via `swift test --parallel`** (188 prior + 2 new compaction edge-case tests).
    - **NICE issues deferred**: N-1 (font bundling) → M12, N-2 (M7→M10 markdown swap is clean), N-3 (Regenerate button no-op until M12), N-4 (`SparkIcon` `.color(.primary)` reads accent via foreground role — works correctly), N-5 (`ChatVerbosity` doc says "Lives in Core" — pre-existing stale doc, fix in M12 doc pass), N-6 (`%.1fK` at zero) cosmetic, N-7 (`ensureConversation` heuristic) gets the M8 comment.
    - **SHOULD items still open**: S-3 (`ChatSessionDriver.stream(text:model:temperature:)` rename + thread temperature) lands with M9 Settings since temperature is the user-adjustable knob. S-4 (per-byte scroll thrash) deferred — practically fine on iPhone 17, real-world only matters on slower devices we don't yet target. S-6 (late textDelta after `.assistantMessageSaved`) hasn't been observed in fixtures; will revisit if it shows up. S-7 (Enter submits on software keyboard) deferred to M11 voice/keyboard polish — TextField with `axis: .vertical` doesn't honor `submitLabel(.send)` on iOS, design's "Enter submits" is a hardware-keyboard-only behavior today. S-8 was a self-confirmation. S-9 `.serialized` removal — left in place; the SnapshotTesting library benefits from serial run within a suite.
  - Tests (190 Chat tests across 27 suites pass via `swift test --parallel`):
    - Behavioral, run on macOS:
      - `UI/Theme/OKLCHTests.swift` (9 cases — white/black anchors, mid-gray achromaticity, gamut clamping, theme `isDark` flags, accent-hue parameterization, display-name strings).
      - `UI/ChatEmptyStateViewTests.swift` (7 cases — late-night, morning, afternoon, evening, late-evening rolloff, 5 AM and 17:00 boundaries).
      - `UI/ChatScreenViewModelProjectionTests.swift` (7 cases — user+assistant projection, tool result inlining via `toolCallId` lookup, compaction banner placement after the cutoff message id, **banner emits when cutoff lands on a dropped tool row**, **banner emits at the tail when cutoff is the last message**, system-row drop, status mapping for pending/failed).
      - `UI/ChatScreenViewModelTests.swift` (5 cases — streaming text accumulates and clears on completion, error event surfaces as banner, no-model send no-op, whitespace trim, compaction tail flag).
    - iOS-gated snapshot suites (`#if canImport(UIKit)`, run via `xcodebuild -scheme Chat -destination 'platform=iOS Simulator,...' test`):
      - `UI/Snapshots/ChatHeaderSnapshotTests.swift` (5 cases — light, dark, sepia, long-title truncation, **XXL**).
      - `UI/Snapshots/ChatEmptyStateSnapshotTests.swift` (5 cases — morning/afternoon/evening/tonight + **XXL**).
      - `UI/Snapshots/ContextMeterSnapshotTests.swift` (4 cases — empty, half-full, over-budget, **XXL**).
      - `UI/Snapshots/ChatComposerSnapshotTests.swift` (7 cases — empty/typed/streaming × themes, near-max context, **XXL**).
      - `UI/Snapshots/MessageListViewSnapshotTests.swift` (7 cases — populated × 3 themes, streaming tail, error banner, compaction banner, **XXL**).
      - `UI/Snapshots/ChatScreenSnapshotTests.swift` (6 cases — empty × 3 themes, populated × 2 themes, **XXL**).
      - `UI/Support/SnapshotEnvironment.swift` — `SNAPSHOT_RECORD=1` env var gate per the plan.
      - 34 baselines under `Tests/ChatTests/UI/Snapshots/__Snapshots__/` (re-recorded after reviewer fixes; clean directory names per `testName: #function` threading).
  - `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
  - **Known issue (deferred)**: an intermittent FakeLLMProvider race exists in single-process `swift test` runs (an existing `ChatSession` task can fire one extra `provider.stream(...)` call after a test exits, hitting the helper's `fatalError` for "no enqueued script"). `swift test --parallel` (separate processes) is consistently green. Pre-existing to M7 — exposed more often as the test count grows. The fix is in test infra (relax the strict fatal in cleanup paths, or add explicit `await session.cancel(); waitUntilFinished()` to all async session tests' teardown). Tracking for M12 polish.
  - **Known gap (deferred)**: custom fonts (`Geist`, `Instrument Serif`, `JetBrainsMono-Regular`) are **not bundled** as resources in the Chat package — `Font.custom(...)` calls fall back to system serif/sans/mono. The empty-state greeting still renders in system serif rather than Instrument Serif. Bundling the font files lands in M12 polish per the plan's `Critical Files to Create`.
  - **Known gap (deferred)**: the M6 `AppBootstrap` integration test was never added (see M6 §Known gap). Still cheap to write thanks to the keychain seam; M8 or M12 will land an `App/Tests/` target.
  - Documentation: `///` doc comments on every public declaration; Chat module's `CLAUDE.md` already documented the UI/ + ViewModels/ folders. Acronym expansions on first use per project doc rules: OKLCH (Oklab Lightness-Chroma-Hue).

## M8 — Sidebar drawer

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-04-26
- **Notes**:
  - `SidebarDrawer` lives at `Packages/Chat/Sources/Chat/UI/SidebarDrawer.swift`. 300pt left-anchored overlay matching `.design-tmp/chat/project/src/sidebar.jsx`. ZStack composition: full-screen 30% black scrim + the drawer surface itself, both gated on `isPresented` so the host just toggles a binding. Scrim taps and the drawer's destructive actions (`onSelectConversation`, `onNewChat`, `onOpenSettings`, `onSelectApplet`) all close the drawer before invoking the callback. `transition(.move(edge: .leading))` + `.easeOut(0.22)` slide-in.
  - Drawer body: serif "Super" wordmark + `v{appInfo.version} · personal` mono caption (60pt top inset matching the design's status-bar offset); accent-filled "New Chat" CTA at the top of a `LazyVStack`; four placeholder applet rows (Todo / Recipes / Bible / Finance) using a custom `SidebarPressableRowStyle` that tints the row with `theme.backgroundSunken` while pressed (mirroring the React `:hover` `--bg-sunken`); CHATS section label; `ChatRowView` per conversation with active row painted in `theme.accentSoft + accent` ink and an inline `SpinnerRing` (1.5pt border + rotating 90° accent wedge) when the row is in `runningSource()`'s set. Footer: identity capsule (BW circle + "Brian Wang" footnote) + 44pt accent-filled gear button. The footer is `.overlay(alignment: .bottom)` so it floats above the scroll content; the scroll has 110pt bottom padding to keep the last chat visible above the footer.
  - Applet glyphs land in `Packages/Chat/Sources/Chat/UI/Icons/SidebarIcons.swift`. Each glyph is a `Shape` driven by SVG-derived 24-unit coordinates piped through a `scaled(_:in:)` helper; the wrapper `StrokedGlyph` strokes them with rounded line caps + joins at 1.5pt. Mirrors `IconTodo / IconRecipe / IconBible / IconFinance / IconNewChat / IconSettings` from `icons.jsx`. `IconSpark` already lived in `SparkIcon.swift` from M7.
  - View model: `Packages/Chat/Sources/Chat/ViewModels/SidebarViewModel.swift` (`@Observable @MainActor final class`). Exposes `chats: [ChatRow]` (sorted desc by `updatedAt`, `nil`/empty titles fall back to "New chat") and `activeConversationId`. Two initializers: production takes `ChatSessionStore` + reads `runningConversations()`; the test-friendly overload takes `runningSource: @Sendable () async -> [String]` so unit tests can drive the running set without spinning up a session. `_setSnapshotState(...)` is the (internal) snapshot-test seam.
  - Composition root wired in `App/ContentView.swift`. `ChatHostView` now holds `@State sidebarOpen`, `@State sidebarViewModel`, and `@State activeConversationId`. The hamburger tap calls `openSidebar()` which sets the binding + kicks off a `refresh()`. Selecting a conversation rebuilds `ChatScreenViewModel` (so transcript, model pill, and used-tokens reset). Starting a new chat creates a fresh `ConversationRecord` with title "New chat" and rebuilds the view model. Settings + applet taps are no-ops pending M9 / future applet work. The drawer overlays the chat in a top-level `ZStack` so it slides over (not under) the chat content.
  - Tests:
    - Behavioral (macOS, run by `swift test --parallel`): `Tests/ChatTests/UI/SidebarViewModelTests.swift` — 5 cases (sort by `updatedAt desc`, nil/empty title fallback, repository-error preserves prior `chats`, running set projects to `ChatRow.running`, `activeConversationId` mutability).
    - iOS-gated snapshots: `Tests/ChatTests/UI/Snapshots/SidebarDrawerSnapshotTests.swift` — 7 cases (open empty light, open populated × 3 themes, active row highlighted dark, running spinner light, Dynamic Type XXL light). 7 baselines recorded under `Tests/ChatTests/UI/Snapshots/__Snapshots__/SidebarDrawerSnapshotTests/` on iPhone 17 (iOS 26.3) sim.
  - Verifications: `swift test --parallel` from `Packages/Chat/` passes **195 tests across 28 suites** (190 prior + 5 new sidebar VM cases). `xcodebuild test -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' -only-testing:ChatTests/SidebarDrawerSnapshotTests` is green. `xcodebuild -scheme Super -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build` succeeds.
  - **Reviewer fixes applied (2026-04-26)**: code-reviewer subagent flagged 1 MUST and 8 SHOULDs. Applied:
    - **MUST-1 streaming-task lifecycle**: `ChatHostView.rebuildChatViewModel(for:)` now calls `viewModel?.cancelStreaming()` on the outgoing view model before swapping in a new one, so a switched-away conversation's stream is terminated rather than orphaning a `Task` that no-ops against a dropped `weak self`. Documented as M11 follow-up to add proper resume-streaming UX.
    - **SHOULD-1 Reduce Motion punt**: `SidebarDrawerSnapshotTests` gained the same documentation block `MessageListViewSnapshotTests` carries about `\.accessibilityReduceMotion` being read-only and the steady-state first frame being identical to the animated one. Future SDK revisit if a writable seam appears.
    - **SHOULD-2 doc compliance**: every `public` declaration on `SidebarDrawer` (init + 9 properties + `AppletDestination` cases) and `SidebarViewModel` (`chats`, `activeConversationId`) now ships with a `///` comment. Acronym expansions on first use: SVG → Scalable Vector Graphics in `SidebarIcons.swift`, CTA → call-to-action in `SidebarDrawer.swift`. The drawer's "callback fires *after* dismissal animation begins" contract is now documented on each callback.
    - **SHOULD-4 title fallback cleanup**: replaced `record.title?.isEmpty == false ? record.title! : "New chat"` (force-unwrap inside ternary) with an explicit `if let raw = record.title, !raw.isEmpty` block.
    - **SHOULD-5 VoiceOver dismissal**: `drawerSurface` gained `.accessibilityAction(.escape) { close() }` so VoiceOver users can dismiss with the two-finger Z gesture.
    - **SHOULD-6 applet enum location**: `AppletDestination` enum now carries an explicit "**MVP shortcut**" doc note flagging the Chat-vs-Shell layering shortcut and pointing at `docs/MOBILE_ARCHITECTURE.md` §applets for the eventual `[SidebarApplet]` data-shape refactor. Each case got a one-liner doc.
    - **SHOULD-7 doc-vs-reality**: `SidebarViewModel` doc no longer claims a "low-frequency timer" that doesn't exist; rewritten to honestly describe "refreshes only when the host calls `refresh()`" with the M9 GRDBQuery upgrade tracked.
    - **SHOULD-3 + SHOULD-8 deferred**: production `runningSource` integration test (SHOULD-3) needs a slow-streaming `MockLLMProvider` test fixture — bigger than M8, deferred to M12 polish. Snapshot helper deduplication (SHOULD-8) is module-internal cleanup with no functional impact, also deferred.
    - **NICE items deferred**: NICE-1 (footer height measurement) cosmetic; NICE-2 (gear-shape simplification doc tightening) one-line; NICE-3 (`.tint`/`foregroundStyle` indirection) read-clarity only; NICE-4 (`contentShape` redundancy) micro; NICE-5 (`SuperAppInfo.fromBundle()` re-read) negligible perf; NICE-6 (wordmark padding doc phrasing). All M12.
  - Re-verifications after fixes: `swift test --parallel` 195/28 still green; `xcodebuild test ... -only-testing:ChatTests/SidebarDrawerSnapshotTests` 7/7 green; `xcodebuild -scheme Super build` succeeds.

## M9 — Settings

- **Checkbox**: `[~]` in_progress
- **Status**: `in_progress`
- **Last updated**: 2026-04-27
- **Notes**:
  - Settings layer split across new files. **Persistence**: `Sources/Chat/Settings/ChatSettings.swift` (typed snapshot — `themeId`, `systemPrompt`, `defaultVerbosity`, `fontScale`, `density`, `autoCompactEnabled`, `autoCompactThreshold`; clamping helpers; nested `ThemeID` + `Density` enums) and `Sources/Chat/Settings/ChatSettingsStore.swift` (wraps `SettingRepository`; `Keys` namespace centralizes the string keys; per-model enabled flag also persisted here). **View model**: `Sources/Chat/ViewModels/SettingsViewModel.swift` (`@Observable @MainActor public final class`). Owns `settings`, `models: [ModelRow]`, `tools: [ToolRow]`, `chatCount`, plus the injected `accountEmail` + `appInfo` chrome. `load()` is idempotent (`hasLoaded` flag) and runs the four reads in parallel via `async let`. Mutators write through to the store/registry on the same call. **Snapshot test seam** `_setSnapshotState(...)` flips `hasLoaded` so the sheet's `.task` doesn't race over it.
  - **Sheet**: `Sources/Chat/UI/Settings/SettingsSheet.swift` is the modal container. Custom bottom sheet (25% black scrim, 22pt top corners, 40pt top inset, slide-up via `.timingCurve(0.32, 0.72, 0, 1, duration: 0.30)`) with a Reduce-Motion fallback to opacity fade. `Pane` enum drives sub-pane navigation; the public init starts at `.root`, an internal `init(initialPane:)` is the snapshot test seam. `SuperTheme.make(_: ChatSettings.ThemeID)` is a public bridge extension that maps the persisted enum to the palette factory.
  - **Shared components** under `Sources/Chat/UI/Settings/Components/`: `SettingsRow` (icon · label · trailing value/affordance, relative font so Dynamic Type scales, `accessibilityHint:` slot for destructive rows, icon hidden from a11y), `SettingsGroup` (rounded card), `SettingsToggle` (44×26 custom switch with white thumb; takes a true two-way binding plus required `accessibilityLabel`), `SettingsHeader` (32pt circular back/close button + centered title + hidden trailing spacer that mirrors the React centering trick).
  - **Glyphs**: `Sources/Chat/UI/Icons/SettingsPaneIcons.swift` ships the chrome glyphs (close X, back chevron, forward chevron, plus, check) plus eight 22pt row-leading glyphs (`Profile`, `Models`, `Theme`, `Prompt`, `Verbosity`, `Appearance`, `Tools`, `Compaction`, `Data`, `About`). Each is a `Shape` driven by 24-unit SVG (Scalable Vector Graphics) coordinates piped through a `sscale(_:in:)` helper; Tools + Compaction are bespoke since they aren't in `settings.jsx`.
  - **Panes** (one file each under `Sources/Chat/UI/Settings/Panes/`): `Settings{Root, Models, Theme, Prompt, Verbosity, Appearance, Tools, Compaction, Data, About}Pane.swift`. Visual references mirror `settings.jsx` 1:1 for the 7 panes that have a React analogue; Tools (one row per registered tool, toggle) and Compaction (auto-compact toggle + threshold slider) ship in the same visual language. Sliders commit on `onEditingChanged(false)` — local `@State` mirrors keep the value live during drag without firing one GRDB write per step. `SettingsPromptPane` commits on focus loss + `.onDisappear` instead of per-keystroke. `SettingsDataPane` uses `confirmationDialog` with a destructive role; the row carries an `accessibilityHint` so VoiceOver users hear "Deletes every conversation. Cannot be undone." before activating.
  - **Composition root**: `App/ContentView.swift` now holds `@State settingsViewModel`, `@State settingsOpen`, the `SettingsSheet` overlay in the same ZStack as the sidebar, and an `.onChange(of: settingsViewModel?.settings.themeId)` that mirrors theme selection back into the host's render theme. The hamburger's "open settings" callback is now wired (`openSettings()`); `rebuildChatViewModel(for:)` reads `settingsViewModel?.settings.defaultVerbosity` so newly created chats start in the user's preferred verbosity.
  - **Wiring scope**: Theme + default verbosity are live. Persistence for `systemPrompt`, `fontScale`, `density`, `autoCompactEnabled`, and `autoCompactThreshold` lands in M9; the consumers (`SystemPromptBuilder`, font-scale env modifier, density tokens, `Compactor` runtime gate) are tracked for M10/M12 polish per their respective doc comments. `IMPLEMENTATION_STATUS.md` reflects this honestly so a future reader doesn't expect them to be hot.
  - **Tests**:
    - Behavioral: `Tests/ChatTests/UI/Settings/SettingsViewModelTests.swift` — 12 tests covering load defaults, every persisted-value read path, every setter (including clamping at both bounds for font scale + threshold), model toggle persistence, tool toggle through `ToolRegistry`, `clearChatHistory` soft-delete sweep, and the `monogram` + `shortEndpoint` helpers (with separator coverage and an unsupported-scheme passthrough case).
    - iOS-gated snapshots: `Tests/ChatTests/UI/Snapshots/SettingsSheetSnapshotTests.swift` — 13 baselines (root × 3 themes, every sub-pane in light, plus Dynamic Type XXL on root) recorded under `Tests/ChatTests/UI/Snapshots/__Snapshots__/SettingsSheetSnapshotTests/` on iPhone 17 (iOS 26.3) sim. Reduce Motion punt is documented inline mirroring `MessageListView` / `SidebarDrawer`.
  - **Verifications**: `swift test --parallel` from `Packages/Chat/` passes **207 tests across 29 suites** (195 prior + 12 new settings VM cases). `xcodebuild test -scheme Chat ... -only-testing:ChatTests/SettingsSheetSnapshotTests` is green (13/13). `xcodebuild test -scheme Chat ... -only-testing:ChatTests/SidebarDrawerSnapshotTests` re-verified green (no regression from the new `SuperTheme.make(_:ChatSettings.ThemeID)` overload). `xcodebuild -scheme Super build` succeeds. App launched on iPhone 17 sim (UUID `472D292D-71F0-4D2B-ADFC-C5D5BAF14450`), PID 94815.
  - **Reviewer fixes applied (2026-04-27)**: code-reviewer subagent flagged 5 MUST and 9 SHOULD findings. Applied:
    - **MUST-1 Dynamic Type**: `SettingsRow` body switched from `.font(.system(size: 15.5))` to `.font(.system(.callout))` so iOS scales row labels with the accessibility size category. Monogram tile + About wordmark stay fixed-size by design (tile would break layout, wordmark is brand chrome). XXL snapshot now produces a visibly different image.
    - **MUST-2 toggle accessibility label**: `SettingsToggle` now requires an `accessibilityLabel:` parameter — every call site (Models, Tools, Compaction) passes the row's name so VoiceOver users hear "Opus 4.7, On" instead of "On, button".
    - **MUST-3 keystroke writes**: `SettingsPromptPane` no longer fires a GRDB write per keystroke. Commits on `FocusState` loss and on `.onDisappear`. The `hasLoaded` guard prevents the initial seed from writing the value back to itself.
    - **MUST-4 unwired knobs**: `defaultVerbosity` now flows into `ChatHostView.rebuildChatViewModel(for:)`. Doc comments on the other knobs (`systemPrompt`, `fontScale`, `density`, `autoCompactEnabled`/`Threshold`) updated to honestly state which milestone wires them.
    - **MUST-5 toggle race**: dropped the dual `Binding(set: { _ in })` + `onChange:` pattern. Toggles take a real two-way binding into the view model so the visible thumb position never races the async write.
    - **SHOULD-6 + 10 load idempotency + parallel reads**: `SettingsViewModel.load()` is a no-op after the first successful load and runs the four datasource reads concurrently via `async let`.
    - **SHOULD-7 slider commit-on-release**: Appearance + Compaction sliders use local `@State` mirrors and `onEditingChanged` so a single drag → 1 GRDB write instead of 7-10.
    - **SHOULD-8 a11y row icons**: `SettingsRow` icons get `.accessibilityHidden(true)` so the leading glyphs don't clutter VoiceOver row announcements.
    - **SHOULD-9 destructive Clear hint**: Data pane row now carries an `accessibilityHint:` so the destructive intent is announced before activation.
    - **SHOULD-11 doc Keys**: every `ChatSettingsStore.Keys` constant gained a one-line `///` describing the encoded value.
    - **SHOULD-12 monogram tests**: added `claude-3-opus` (split on `-`) and `gpt_4o_mini` (split on `_`) cases plus a `file://` scheme passthrough in `shortEndpoint`.
    - **SHOULD-13 dead `.task` else-branch**: removed — the `if isPresented` already destroys the subtree on dismiss, so `pane = .root` was unreachable. Comment updated to explain the implicit reset.
    - **SHOULD-14 Density TODO**: doc comment on `Density` enum now explicitly says spacing-token consumers ship in M12.
    - **NICE-16**: dropped the `_ = headStart` dead binding in `ToolsGlyphShape`.
    - **SHOULDs deferred**: SHOULD-15 (`SettingsSheet.Pane.title` localization), SHOULD-19 (dark scrim contrast), SHOULD-20 (`Density` extra-test) — all small polish, queued for M12.
  - Re-verifications after fixes: `swift test --parallel` 207/29 still green; `xcodebuild test ... -only-testing:ChatTests/SettingsSheetSnapshotTests` 13/13 green; `xcodebuild -scheme Super build` succeeds.
  - **Follow-up (2026-04-28) — navigation paths + model detail pane**: user feedback "Add Model does nothing; pane transitions should push; settings need built-in deep links."
    - **Pane navigation refactor**: `SettingsSheet.Pane` switched from `String, CaseIterable` to a plain `Hashable, Sendable` enum so it can carry `case modelDetail(id: String?)`. `SettingsViewModel` gained `navigationPath: [Pane]` plus `openPane(_:) / popPane() / popToRoot()` helpers. `SettingsSheet` content is now wrapped in a `NavigationStack(path: $viewModel.navigationPath)` so push/pop animations are native, and the system nav bar is hidden via a tiny `HiddenNavigationBarModifier` (gated `#if os(iOS)` to keep the macOS build green). The view model's path is the integration point — external callers (e.g. a future chat-side affordance) can deep-link with `viewModel.openPane(.modelDetail(id: nil))` and the sheet will route automatically. The internal `init(initialPane:)` test seam now seeds `viewModel.navigationPath`.
    - **Model add/edit pane**: new `Sources/Chat/UI/Settings/Panes/SettingsModelDetailPane.swift` — form for Name, Base URL, Model ID, API Key (`SecureField`), Supports thinking toggle, Max context tokens. `editingId == nil` ⇒ blank form + "Add Model" CTA; non-nil ⇒ form is seeded from the in-memory `ModelRow` at init time (avoids `.onAppear` race that left the toggle unset in snapshots) and a destructive Delete row appears with a `confirmationDialog`. Validation is local: Save stays greyed until name + base URL + model id are non-empty and max-context parses to a positive Int (and, in create mode, an API key was supplied). `SettingsKeyboard` is a small platform-agnostic enum so `keyboardType(_:)` can stay behind a `#if canImport(UIKit)` branch.
    - **View-model CRUD**: `SettingsViewModel.createModel(...)` / `updateModel(...)` / `deleteModel(id:)`. Create generates a fresh Keychain ref + record id, writes the key, then the record. Update keeps the existing `apiKeyRef` when the key field is blank (so editing without rotating doesn't clear the secret). Delete delegates to `ModelConfigurationRepository.delete(id:)` which already does Keychain-first ordering. All three call `loadModels()` afterward so the UI refreshes immediately. `ModelRow` grew `baseURL`, `modelId`, `supportsThinking` (with defaults so existing test fixtures still compile) so the detail pane can seed without a second repository fetch.
    - **Models pane wiring**: card body and Add CTA now push the detail pane. The toggle stays separate (no row tap collision) — body is wrapped in its own `Button` with a `contentShape(Rectangle())` so the tap region matches the card; the toggle remains a sibling.
    - **Tests**: 5 new view-model cases (`navigationPathHelpers`, `createModelPersists`, `updateModelKeepsRef`, `updateModelRotatesKey`, `deleteModelClears`) bring the suite to 17 settings VM tests. `StubModelRepository` gained a `storedKeys: [String: String]` so the assertions can verify what landed in the Keychain layer. Snapshots: 2 new (`modelDetailEmpty` and `modelDetailEdit`) bring the SettingsSheet baseline count to 15. The seeded "Opus 4.7" fixture row was upgraded with a real `modelId` + `supportsThinking: true` so the edit snapshot exercises a complete form.
    - **Verifications**: `swift test --parallel` 212/29 green. `xcodebuild test -scheme Chat ... -only-testing:ChatTests/SettingsSheetSnapshotTests` 15/15 green on iPhone 17 (UUID `472D292D-71F0-4D2B-ADFC-C5D5BAF14450`). All 13 prior baselines re-recorded once because NavigationStack wrapping introduced sub-pixel anti-aliasing jitter (max 2/255 channel diff in 0.05% of pixels — visually identical, confirmed via PIL). `xcodebuild -scheme Super build` succeeds; build re-installed and launched on the same sim PID.

## M10 — Markdown + code + thinking rendering

- **Checkbox**: `[x]` done
- **Status**: `done`
- **Last updated**: 2026-05-03
- **Notes**: MarkdownUI + Splash wired in via four new files under `Packages/Chat/Sources/Chat/UI/Markdown/`:
  - `MarkdownTheme.swift` — `SuperTheme.markdownTheme()` builds a `MarkdownUI.Theme` painted against the active SuperTheme. Covers `text`, inline `code`, `strong`, `emphasis`, `link`, `heading1/2/3`, `paragraph`, `blockquote`, `listItem`, `table`, `tableCell`, and `codeBlock`. Tables wrap in a horizontal `ScrollView` so wide tables don't clip on a 402pt iPhone. `@MainActor` because the closures call MainActor-isolated SwiftUI modifiers (`markdownMargin`, `markdownTextStyle`, `relativeLineSpacing`).
  - `SplashHighlighter.swift` — Splash → SwiftUI `Text` adapter. `CodePalette.from(SuperTheme)` derives per-token colors from the design's `--code-kw / --code-str / --code-num / --code-cmt` tokens. Splash only ships Swift grammar — non-Swift fences degrade to plain code-foreground monospaced text via `addPlainText`. Lives outside `MarkdownUI.CodeSyntaxHighlighter` because `CodeBlockView` overrides the entire code-block slot. Disambiguates `Color` to `SwiftUI.Color` since Splash exports its own `Color` type.
  - `CodeBlockView.swift` — Dark surface, lang label + copy pill in header, monospaced body painted by `SplashHighlighter`, horizontal `ScrollView` so long lines scroll instead of wrapping. Matches `.design-tmp/chat/project/src/message-parts.jsx`.
  - `MarkdownText.swift` — Wrapper around `Markdown(_:)` that pulls the active `SuperTheme` from `@Environment` and applies `markdownTheme()`. `BodyStyle` enum lets `ThinkingBlockView` (italic + softer ink) and `CompactionBannerView` (smaller body) share one wrapper instead of forking three theme builders.
- **Call-site swaps in `MessageListView.swift`**: `AssistantMessageView` body, `ThinkingBlockView` body, and `CompactionBannerView` summary now use `MarkdownText`. `StreamingTailView` intentionally keeps plain `Text` so partial markdown doesn't re-parse on every delta — the cutover happens in `ChatScreenViewModel.handle(.assistantMessageSaved)` which writes the persisted row that then renders through MarkdownUI.
- **Verbosity semantics** (`.simple` collapses thinking+tool, `.thinking` expands thinking only, `.verbose` expands both, with user-tap override) were already implemented in M7 and unchanged.
- **Tests**: 6 new unit tests (`SplashHighlighterTests` × 4, `MarkdownThemeTests` × 2) cover palette wiring, grammar gating, and theme builder construction. 3 new snapshot baselines (`list_markdown_light`, `list_codeblock_light`, `list_table_light`) cover paragraphs/bold/inline-code/list/h3, a fenced ```swift block with Splash highlighting, and a GFM table rendered through the chrome. `swift test --parallel` 241/32 green; `xcodebuild test -scheme Chat -destination 'platform=iOS Simulator,id=472D292D-71F0-4D2B-ADFC-C5D5BAF14450'` 300/40 green. All 47 prior snapshot baselines re-recorded once because the iOS sim runtime updated since they were captured (PNG-encoding-only drift — PIL diff confirmed every failing image was pixel-identical to its baseline). Pass `TEST_RUNNER_SNAPSHOT_RECORD=1` (not `SNAPSHOT_RECORD=1`) to re-record via xcodebuild — the `TEST_RUNNER_` prefix is what xcodebuild forwards into the iOS test process. `xcodebuild -scheme Super build` succeeds; installed + relaunched on iPhone 17 sim (UUID `472D292D-71F0-4D2B-ADFC-C5D5BAF14450`).

## M11 — Voice input

- **Checkbox**: `[ ]` not_started
- **Status**: `not_started`
- **Last updated**: —
- **Notes**: `SFSpeechRecognizer` on-device only. Invoked by the composer's trailing button when the field is empty. `NSSpeechRecognitionUsageDescription` + `NSMicrophoneUsageDescription` added to Info.plist.

## M12 — End-to-end polish + coverage

- **Checkbox**: `[ ]` not_started
- **Status**: `not_started`
- **Last updated**: —
- **Notes**: Coverage thresholds per AGENTS.md (Core ≥80%, applets ≥70%). Doc updates: add `ToolRegistration`, `ChatSessionStore`, `ContextAssembler`, `Compactor`, `CompactionCheckpointRecord`, `.thinkingDelta`, `.compactionStarted`/`.compactionCompleted` to the architecture docs; update `CLIENT_SERVER.md` to describe the MVP "no-server" mode.

---

## Notes for future sessions

- **Never rename files listed in the plan's Critical Files list without updating both the plan and this file.** Agents relying on grepped paths break silently otherwise.
- **Never re-record snapshot tests to "make them pass"** — per AGENTS.md §Testing, only re-record when the visual change is intentional and explained in the PR.
- **Never skip hooks** (`--no-verify`) or bypass tests — per AGENTS.md.
- **Module-boundary rule**: Chat imports Core. App imports Chat + Core. Nothing else. No cross-applet imports (though there's only one applet in MVP).
- **Never commit without running `swift test` in each touched package locally** — per AGENTS.md §Testing.3.
